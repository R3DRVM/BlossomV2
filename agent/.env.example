# Blossom Agent Environment Variables

# Server port (default: 3001)
PORT=3001

# LLM Provider: 'openai' | 'anthropic' | 'stub' (default: 'stub' if not set)
BLOSSOM_MODEL_PROVIDER=openai

# OpenAI Configuration
BLOSSOM_OPENAI_API_KEY=sk-your-key-here
BLOSSOM_OPENAI_MODEL=gpt-4o-mini

# Anthropic Configuration (alternative to OpenAI)
# BLOSSOM_ANTHROPIC_API_KEY=sk-ant-your-key-here
# BLOSSOM_ANTHROPIC_MODEL=claude-3-5-sonnet-20241022

# Note: If no provider/key is set, the backend runs in stub mode,
# returning canned responses with no actions.
